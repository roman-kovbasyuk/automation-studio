# Catalog copy mode

Approved behavior: opt-in sidebar mode; clicking specimens copies `use this element from design system: {classes and modifiers}`. Shared controls retain their normal interaction contract outside catalog inspection.

Implementation: catalog-only hook captures pointer and keyboard activation before examples act. Sidebar and header filters remain usable. Copies canonical a-/b-/c- classes and visual data modifiers, plus icon identity/size when inspecting icons. Clipboard failure provides a manual-copy reference.

Verification: 73 tests pass, including clipboard contents and activation suppression/restoration. Typecheck and catalog build pass. Live UI reports copy success and normal dropdown interaction returns when disabled. Browser clipboard-read tool returned an older value despite the page success state, so native clipboard contents were not independently confirmed. Existing bundle warning remains.
